name: 📊 Daily Signal Check (통합)

on:
  # 매일 자동 실행 (뉴욕 시간 오후 5:00 = UTC 22:00)
  # 기존 6개 워크플로우가 21:30~21:55에 실행되므로, 그 이후 시간
  schedule:
    - cron: '0 22 * * 1-5'  # 월-금, UTC 22:00 (뉴욕 5:00 PM)
  
  # 수동 실행도 가능
  workflow_dispatch:

jobs:
  check-all-signals:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run integrated signal check and send email
      env:
        EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        EMAIL_TO: ${{ secrets.EMAIL_TO }}
      run: python scripts/daily_check_all.py
